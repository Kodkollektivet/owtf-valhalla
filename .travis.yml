sudo: required

language: python

python:
  - "2.7"

services:
  - docker

before_install:
  - sudo apt-get update
  - sudo apt-get install -y -o Dpkg::Options::="--force-confdef" -o Dpkg::Options::="--force-confold" docker-engine=1.10.1-0~trusty

install: pip install -r requirements.txt

<<<<<<< 8344a0ba4e11821efd817319574609fcf3d53e99
script: ./owtf.sh test
=======
install: python ./owtf.sh setup
script: python ./owtf.sh test
>>>>>>> Fixed travis install and test script calls

branches:
  only:
    - master
